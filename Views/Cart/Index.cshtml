@model List<CartItem>

<h1>Ваша корзина</h1>

<p>Items in Cart: @(Context.Session.GetInt32("CartItemCount") ?? 0)</p>

<link rel="stylesheet" href="~/css/cart.css" />

@if (Model.Any())
{
    @foreach (var item in Model)
    {
        <div class="product-card">
            <h3>@item.Product.Name</h3>
            <p>@item.Product.Description</p>
            <p>@item.Product.Price ₽</p>
            <p>В наличии: @item.Product.InStock </p>
            <img class="product-image" src="@item.Product.ImageUrl" alt="@item.Product.Name" />
            <p>Добавлено: @item.Quantity</p>
        </div>
    }
}
else
{
    <p>Добавьте продукты либо зарегистрируйтесь</p>
}

<link rel="stylesheet" href="~/css/shared.css" />
